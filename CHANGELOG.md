# 更新日志

本文件遵循 [Keep a Changelog](https://keepachangelog.com/) 格式，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 规划中
- 验证中心 L4 引擎与 Lean 4 / mathlib 的绑定
- 难题档案的自动状态追踪（监听 arXiv 与期刊）
- 知识图谱可视化（跨分支依赖关系）
- 进一步扩域：群阶 >16（17–31 阶需素数阶与更多半直积构造）、算术函数 >500、划分 >200
- 恒等式验证器仍拒答的几类，按「能不能真判」排序待办：
  ① 条件式全称命题（`whenever not(a=0) then a/a = 1`）——需要在抽样时按守卫条件
     过滤样本点，可行但要定义"守卫为真却仍不符"才算反例，口径需先写清楚；
  ② 存在量词句（`There does not exist a c>0 such that …`）——有限抽样**证不了**
     不存在，只能证"没找到"，与 falsification 通道的语义冲突，暂不纳入；
  ③ 散文式描述（`e = the sum as j ranges from 0 to infinity of 1/(j!)`）——
     需要符号求和能力，超出抽样验证的范畴。
- C4 参数已按对照实验定在 `P_ORDER_MAX=4` / `P_DEG_MAX=4`；再往上（order 5）
  在基础库与变换库上均无新增召回，故停在这里。将来若新增校准件，
  须重跑 `tests/c4_ablation.py`（它同时扫基础库与 4 个变换后的库）再动参数。
- 群阶 16 之后：17–31 阶需要素数阶循环群与更多半直积构造，
  子群 join 闭包本身已与阶数无关（实测 19.7x 加速），瓶颈只在构造器。

### 新增（2026-09-20 第六轮·续：千禧难题档案 S10）

新增第 ⑩ 阶段 `06-AI自动化/01-工作流/openmath_millennium.py`，产出
`09-数据/millennium_dossier.json` 与 `MILLENNIUM_REPORT.md`。两个新模块：
`openmath_sys/millennium.py`（命题形式化 + 套娃分解）、
`openmath_sys/millennium_lab.py`（可算实验）。**本轮未声称证明任何一道题。**

- **命题形式化**：七题各自写成逻辑方程（量词结构 / 约束域 / 谓词 / **否定式**）。
  否定式是硬要求——只能证实不能证伪的档案是残缺的（自核验第 2 条专查此项）。
- **无穷套娃分解**：递归子命题树，叶分三类（已证定理 / 可算检验 / 开放叶）。
  深度 2–3，**15 个开放叶如实保留**。PNP 下挂着三条**已证的不可能性定理**
  （Baker–Gill–Solovay 相对化 / Razborov–Rudich 自然证明 / Aaronson–Wigderson 代数化），
  那才是这套分解真正的深度：它不是"还没做"，而是"这类方法已被证明必然失败"。
- **有限影子**：每题配一个有限截断版本，并显式判定「影子 ⟹ 原命题」。
  **七题全部为否**（`n_shadow_implies_full = 0`），这是刻意保留的结论。
- **8 个可算实验**（每题都带 caveat，且都写明对该题本身零证据）：
  ξ 函数方程（校准件）、Hardy Z 零点计数（T=600，341 个）、椭圆曲线点计数与
  Hasse 界（定理，校准件）、一维 Burgers 激波、组合 Hodge、随机 3-SAT 相变、
  S³ 同调（庞加莱已由 Perelman 解决，此处**不重复证明**，只校准同调代码）。
  YM **没有实验**——不是漏做，而是四维量子测度尚未构造，不存在可计算的谱。

#### 审计新增「千禧」维度（288 项）

每题的可算部分都用**另一套算法**重算：
- ζ：Euler–Maclaurin vs **η 函数 + Euler 变换**（无任何 Bernoulli 数、不做尾部积分）
- θ：Stirling 闭式 vs **对 θ′ 做 Simpson 求积**（digamma 用递推 + 渐近展开），两法差 1e-14
- 零点计数：Hardy Z 符号变号 vs **辐角原理**（沿 2→2+iT→½+iT 连续跟踪 arg ζ 增量求 S(T)，
  再用 N=θ/π+1+S）。**N(600) = 341.0000，偏离整数 0.0**，与符号变号计数逐位一致；
  S(T)=0.260502 与被测「计数减主项」0.2605 吻合。这是本段最强的对账。
- #E(F_p)：平方表 O(p) vs **(x,y) 暴力枚举 O(p²)**，82 组 0 分歧
- Betti：Fraction 高斯消元 vs **GF(p) 高斯–约当**（面枚举与边界算子也重实现一遍）
- SAT：DPLL vs **2^n 全枚举**，40 例逐例对账
- 激波时刻：数值特征线 vs **解析解** t* = −1/min u₀′ = 1（误差 7.3e-5）

另加一组**诚实性结构检查**（与算数同样重要）：未解命题必须留有开放叶、
有限影子不得被说成蕴含原命题、不得出现证明性措辞、实验 id 必须唯一且每条必带 caveat。

#### 修正

- **恒等式验证器的期望值修正（不是放宽标准）**：
  `sin A cos B + cos A sin B = sin(A + B)` 原本在**拒答集**里，理由是当时的解析器
  读不了隐式乘法（尾部 token 无法消费 ⇒ 只能拒答）。解析器原生支持隐式乘法后，
  它被正确读成 (sin A)(cos B)+(cos A)(sin B)=sin(A+B)，抽样差 1e-16。
  故**从拒答集移入判准集**（仍是一道回归护栏：判错就会红），
  并以 `f(x) + f(y) = f(x + y)` 补进拒答集——未知函数名**不得**被静默重读成乘积。
  现判准率 35/35 真、11/11 假，拒答 8 条。
- 档案中若干 `shadow_bound` 与实际跑的参数不一致（RH 写 T=200 实际跑 600、
  PNP 写 n≤12 实际相变用 n=40、HODGE 写单形数 ≤40 实际 T² 有 54 个），已逐条改成实测值。
- `millennium_lab.py` 里 `exp_homology_sphere` 被定义了两次（后定义覆盖前定义），
  已删除死代码；Burgers 的 ν=0 / ν=0.05 两次运行原本同 id，已区分。
- 审计里曾把 ζ 的抽样点选在**零点**上（t=14.13、37.59），纯相对误差被 1e-10 的
  分母放大成假失败。标度改为 `max(1,|ζ|)`。

### 新增（2026-09-20 第六轮，子群完备枚举 + C4 对照实验 + 多变量全称式）
- **子群枚举由「枚举全部含单位元子集」换成「循环子群 join 闭包」**
  （`_subgroup_join_closure`）。完备性是**恒等式**而非假设：任意子群
  H = join_{h∈H}⟨h⟩，故从全部循环子群出发反复做 join 到闭包稳定即得全部子群。
  对账护栏：`tests/check_subgroup_closure.py` 在 n≤14 的库内群上与旧指数级枚举
  **逐群比对，0 分歧**；26 个群的子群数与教科书计数全对；耗时 **19.7x** 加速。
- 群库扩到 **16 阶**：新增 `structure.order16_library()`，14 个构造两两可区分
  （指纹 = 交换性/指数/中心阶/共轭类数/子群数），与教科书「16 阶群共 14 个」吻合。
  其中 Z2^4 的子群数 67 = 高斯二项式和 1+15+35+15+1，可作独立校验点。
  `build_group_library(extended=True)` 随之扩到 13–16 阶。
  标注：这是**构造出的 14 个**，不是"证明了只有 14 个"。
- **C4 参数对照实验** `tests/c4_ablation.py`：网格扫描
  (max_order, max_deg) ∈ {1..5}×{0..4}，记录召回 / 假警报 / 耗时。
  新增硬校准件 **`apery`**（Apéry 数 A005259，deg=3 order=2，由教科书闭式
  Σ_k C(n,k)²C(n+k,k)² 独立生成，非抄递推），正好卡在次数上限边界上。
  结论：**只看基础库会得出错误结论**——基础库上 deg 4 与 order 4 均无新增召回，
  但扫完 4 个变换后的 124 条序列后，order 3→4 得 **+6 条**（集中在 `prefix_sum`，
  部分和把递推阶抬高 1 是定理不是拟合）、deg 3→4 得 **+1 条**（`apery@diff`
  实际需要 order=3 / deg=4）。两种放宽的**假警报均为 0**，8 条阴性对照
  （bell/partition × 4 变换）全程静默。据此定 `P_ORDER_MAX=4`、`P_DEG_MAX=4`；
  order 5 在两张库上都没有新增召回，故不再往上。
  这条同时**消掉了 S9 的一处红灯**：`apery` 加入后 `diff` 变换下曾报 1 条丢失，
  定位为次数上限伪影（有对照证据），放宽后 `unexplained_loss` 回到 **0**。
- **恒等式验证器支持多变量全称式**（`lcm(a,b)=a*b/gcd(a,b)` 一类）：
  ① `a`/`an` 从"无条件当作散文词"改为**歧义档**——只有在同句共现硬散文词
     （the/such/that/sum/of…）时才判散文；`the sum of a and b = a+b` 仍拒答。
  ② 新增 `INTEGER_FUNC_NAMES` 与**整数抽样域** [1,60]：gcd/lcm 只在整数上有定义，
     实数抽样会把它们退化成 0/0。域与函数在结论里如实标注（`sampling_domain`）。
     注意 floor/ceil **不**在这张表里——它们在实数上有定义，压成整数反而验不了。
  ③ 新增 `strip_universal_quantifier`：剥掉 `for all a,b | …` 全称前缀，
     约束变量表一并记进结论。只认这一种形式；带存在量词 / 条件句 /
     并列关系的**一律不剥**（剥了抽样的就不是原式）。
  ④ 修正"右侧为常数 0 → 待解方程"的误杀：该规则没区分左侧是否恒为常数，
     把 `for all a | 0*a = 0`、`a+(-a) = 0` 一起拦了。改为用**数值证据**判定
     左侧是否随变量变化；`x^2-5x+6=0` 仍正确判为待解方程。
- 审计规模 4072 → **4163** 项，0 失败。恒等式段 38 → **116** 项，
  群段 383 → **716** 项，序列段 466 → **479** 项。

### 新增（2026-09-19 第五轮，C4 换求解器 + 解析器隐式乘法 + 审计扩域）
- **C4 求解器由「精确有理高斯消元」换成「模素数消元 + 有理重建 + 精确整数复核」**
  （`_nullspace_mod` / `rational_reconstruct` / `_prec_nullspace_candidates`）。
  动机：隔项抽样 `a(2n)` 让项在发现集第 30 项就超过 1e45，Fraction 消元的分母
  指数膨胀，旧版只能「跳过」——这让两组长不出结果的原因变成**我们放弃了**而
  不是**它没有**。换掉后项多大都能算，且因为最后一步是**精确整数复核**，
  放得再宽也不会放进假阳性：模逆或重建失真只导致漏报，错过不去复核。
- 换求解器的当场收益：**上一轮列为「能力缺口」的 2 条丢失全部找回**——
  `even_subseq(a(2n))` 下 `factorial` 得 `a(n)=(4n²−2n)a(n−1)`（即 (2n)!/(2n−2)!，
  可手算验证）、`derangements` 得阶 3 次 2 的递推；`catalan`/`central_binomial`/
  `motzkin` 的隔项版本也一并拿到。`bell`/`partition` 仍然报不出（正确结果）。
  `unexplained_loss` 由 2 → **0**。
- `audit.py` 新增审计第 ⑥ 组「C4 求解器交叉验证」：
  ⑥-a 有理重建单测（5 个已知有理数往返）；⑥-b 模数素数性独立验证；
  **⑥-c 用被换掉的那套精确 Fraction 消元当参照**，28 个序列上两套求解器必须
  给出同一组递推（本轮 0 分歧）；⑥-d 每条候选须由 ≥2 个独立素数共同重建。
  审计规模 1830 → **1885** 项，0 失败。

### 新增（2026-09-19 第四轮，S9 扩到四通道 + 套娃层）
- **C4 多项式系数递推（P-recursive / holonomic）通道** `discover_polynomial_recurrence`：
  搜索 `p_0(n)a(n)+…+p_k(n)a(n-k)=0`（deg p_i ≤ 2、k ≤ 3，精确有理零空间）。
  本轮拿下 **`derangements`**（D(n)=(n−1)(D(n−1)+D(n−2))）与
  **`motzkin`**（(n+2)M(n)=(2n+1)M(n−1)+(3n−3)M(n−2)）——这两条 C1/C2 都拿不到。
  C4 校准：期望阳性 5/5 召回、期望阴性 14/14 真阴性、0 假警报。
  **期望阴性是有理由的**：序列 P-recursive ⟺ 生成函数 D-finite；贝尔数的指数
  生成函数 e^(e^x−1) 与划分数的 1/(q;q)_∞ 都非 D-finite，所以 C4 报不出是正确结果。
- **变换算子下的封闭性（S9 的套娃层）** `transform_closure`：6 个变换 × 30 个序列 = 180 组。
  差分/部分和/二项变换在理论上对 C-finite 与 P-recursive 封闭，实测**真正丢失 0 条**。
  发现不动点：**`powers_of_two` 是差分的不动点**（Δ(2ⁿ)=2ⁿ，可直接验证的恒等式）。
  「丢失」栏新增两步排除：输出退化（逆二项变换把多项式型序列打零）、
  放宽阶数上限后找回（根因是上限而非性质）。剩余 2 条未解释丢失已列为能力缺口。
- **证伪机制元检验** `meta_test_falsification`：见下方「修正」栏第 1 条，
  这是本轮最重要的口径修正。

### 新增（2026-09-19 第三轮，S9 序列理论）
- **06-AI自动化/02-引擎/openmath_sys/sequences.py**：序列理论引擎。在自己构造的
  30 个整数序列 × 60 项上跑三条通道：C1 常系数线性递推（精确有理高斯消元求零空间，
  最高 4 阶）、C2 一阶有理（超几何）闭式 `a(n+1)/a(n)=P(n)/Q(n)`、C3 增长率
  （`log a(n)` 对 `n` 与对 `log n` 双模型回归 + 超指数判据）。发现集只用前 30 项，
  后 30 项仅做外推检验；另用前 15 项跑压力测试暴露小样本过拟合。
- **C1 与 C3 的对账（本轮唯一的方法论新增）**：新增 `characteristic_polynomial` /
  `polynomial_roots`（Durand–Kerner）/ `growth_from_recurrence` / `reconcile_growth`，
  由递推的特征多项式求根反推增长率，与尾部窗口拟合对账。同一序列的增长率第一次有了
  两条互相独立的路径，不一致即说明至少一方有 bug。可对账 11 条，本轮 11/11 一致。
- **06-AI自动化/01-工作流/openmath_sequences.py**（流水线第 9 阶段 S9）：产出
  `09-数据/sequence_theory.json`、`sequence_candidates.json` 与
  `01-工作流/SEQUENCE_REPORT.md`；含 10 项自核验（本轮 0 违规）。
- `audit.py` 新增 `audit_sequences()`：教科书/OEIS 首项对照（参照值不取自本仓库
  任何代码）、每条递推由**另一份实现**独立重算（交叉相乘、无除法）、主特征根对照
  经典常数（φ、tribonacci 常数、塑性数）、C1↔C3 双路径对账。审计规模 1419 → **1798**
  项检查，0 失败。
- `numeric.py` 新增 `normalize_implicit_mul()`：`2z`→`2*z`、`iz`→`i*z`、
  `2 ln(x)`→`2*ln(x)`、括号相邻补 `*`。解析器由此**支持**隐式乘法，不再只是识别它。
- 每日自动流水线由 8 阶段扩展为 **9 阶段**（追加 S9_sequences）。

### 新增（2026-09-19 第二轮，S8 独立审计）
- **06-AI自动化/02-引擎/openmath_sys/audit.py**：独立审计引擎。用**与被测代码不同的
  算法**重算本仓库自己算出的每一个可复核数字：算术函数（暴力定义）、π(x)（教科书值）、
  li(x)（Ei 幂级数 vs Simpson 积分）、p(n)（五边形数递推 vs 直接生成）、
  图不变量（位掩码/回溯/BFS/Bareiss）、有限群（2^n 子集穷举）、链复形同调（独立 GF(p) 消元）、
  以及恒等式验证器的**判准率与拒答率**。本轮 1419 项检查 0 失败。
- **06-AI自动化/01-工作流/openmath_audit.py**（流水线第 8 阶段 S8）：产出
  `09-数据/audit_report.json` 与 `01-工作流/AUDIT_REPORT.md`；含 6 项自核验。
- `numeric.py` 新增 `screen_identity()` 准入筛查：范围判定（级数/积分/微分/集合/量词）、
  变量合理性（散文词 / 函数名 / 多字符标识符）、未识别 token 反向检查、
  隐式乘法检测、待解方程识别；`verify_identity` 新增 `not_decidable` / `inconclusive`
  两种结论，并对多值函数的 fails 附加 `branch_warning`。
- 每日自动流水线由 7 阶段扩展为 **8 阶段**（追加 S8_audit）。

### 修正（2026-09-19 第二轮）
- **判定口径修正（严重）**：`verify_identity` 此前对**超出能力范围**的式子也给出
  holds/fails，制造了 14 条假阴性并写入 `numeric_solutions.json`。实例：`e = the sum as j
  ranges...` 把冠词 `the` 当成变量；`sin A cos B` 把 `sin` 当成变量并被错误嵌套；
  `exp(ix)` 与 `2z` 这类隐式乘法被静默处理成单个标识符或常量；`a*x^2+b*x+c=0`
  （待解方程）被当成恒等式抽样。现在这些一律标记为 `not_decidable`。
  33 条中可判定的只有 10 条（原报告称 14 条"不成立"，实际只有 1 条，且属分支约定问题）。
- **统计口径修正**：`numeric_solutions.json` 的处置率曾把「恒等式通过数 + 求根成功数」
  除以「全部符号失败数」，分子分母来自不同判定通道（算出过 150%）。
  现新增含义单一的 `identity_pass_rate`，旧口径保留为 `resolution_rate` 仅供历史对照。
- **实错修正**：`theoryforge.graph_invariants` 中单点图（P1/K1/S1）的生成树数为 0，
  应为 1（平凡图有恰好 1 棵生成树）。已修正。
- **精度修正**：`numbertheory.logarithmic_integral` 在 x=1e5 处相对误差 2.4e-5
  （对 [10,x] 一次性均匀 Simpson 导致）。改为按 10 倍几何分段加密，误差降到 1e-13 量级。
- 记录一次**审计自身的错误**：曾误判 li(x) 系统性偏大 1.045，实为参考值记错
  （5.120 是 ∫₂¹⁰ dt/ln t，li(10) 真值 6.1656）。该案例已写入 AUDIT_REPORT 第 5 节。

### 修正（2026-09-19 第五轮）
- **归因错误自纠（严重）**：上一轮把 `even_subseq(a(2n))` 下
  `factorial`/`derangements` 找不到递推的原因写成「**次数上限 `P_DEG_MAX=2` 不足**」，
  并据此写进 CHANGELOG 与自动化提示词。实测是错的：真因是**项绝对值上限**
  `P_TERM_ABS_BOUND=1e45` 触发了「跳过，不是找不到」，与 `P_DEG_MAX` 无关
  （同样 b(1)/b(0)=2 可由 deg=2 表达，原来的上限本来就够）。
  教训：**未查实就把能力缺口归类为某个参数不足，等于把「我们放弃了」说成
  「它没有」**——这比漏报更危险，因为它会误导后续投入方向。
  现改为「换求解器」并加两道防线：一是换完**用被换掉的那套当参照**
  （审计 ⑥-c），二是每条候选必须由 ≥2 个独立素数共同重建（⑥-d）。

### 修正（2026-09-19 第四轮）
- **口径修正（严重，本轮最重要）**：报告中「外推证伪 0」此前会被读成「没有过拟合」，
  真相是 **C1/C2 只报发现集上精确相容且解唯一的解**，不相容的序列连候选都报不出来，
  根本走不到外推检验那一步——`FALSIFIED_BY_EXTRAPOLATION` 在真实序列上**结构上不可达**
  （实测：60 项随机噪声 → C1 报 0 条、C2 报 0 条；300 个随机序列同样 0 条）。
  现新增 `meta_test_falsification()`：自造「前 k 项满足、第 break_at 项起破功」的
  合成序列丢进引擎，8/8 精确定位到 break_at，证明通道本身是通的。
  报告与审计均已加「0 证伪的正确解读」说明。
- **召回率口径修正**：C1/C2 的召回率一度算出 **1.2**（>1），因为把 C4 认领的
  `derangements`/`motzkin` 也算进了 C1/C2 的分子。现已按通道分开统计；
  同时新增 `n_sequences_with_known`（按序列计）与 `n_known_rediscovered`（按候选计）
  两个口径，避免同一条递推被两条通道重复计数。
- **候选数虚高修正**：C4 会把 C1 已报出的常系数递推用 deg=0 多项式重写一遍，
  候选数一度 4 → 17。新增 `_mark_redundant()`：把这类形式标为
  `CONSEQUENCE_OF_C1` / `CONSEQUENCE_OF_C2`，不计入候选（本轮标记 16 条）。
  已知形式的判定**优先于**冗余判定，否则会丢掉 catalan/central_binomial/factorial
  三条 C4 召回（它们是教科书已知、同时也是 C2 的推论）。
- **期望阴性集口径修正**：初版把 `derangements`、`motzkin` 归入「期望阴性」，
  注释写「它们本就不满足常系数/超几何递推」——那句对，但把它们当「无结构」是错的，
  加 C4 后立刻自相矛盾。现已按通道分开登记，并在报告中写明「阴性必须指明对哪个通道」。
- **常数序列漏检**：`a(n)=a(n-1)` 在 k=1 仿射下欠定（所有方程线性等价），
  被「要求解唯一」直接过滤，常数序列一个候选都不报。现已显式报出并标 `DEGENERATE`。
- **命名冲突修正（隐患）**：`openmath_sys/__init__.py` 把 `sequences.discover_linear`
  扁平导入，**静默遮蔽**了 `theoryforge.discover_linear`（两者语义完全不同）。
  现已不再扁平导入任何重名函数，序列侧重名函数一律走 `openmath_sys.seq.<name>`。
- **审计自身的 bug**：在列表推导里每次新建 `random.Random(seed)`，得到的是
  **常数序列**，而常数序列当然满足 `a(n)=a(n-1)`——审计自己造出了一个假警报。
  已改为用同一个 Random 实例，并固定种子保证可复现。

### 修正（2026-09-19 第三轮）
- **实错修正（由新加的对账打出）**：`growth_from_recurrence` 初版把「主根模为 1、
  常数强迫」情形的多项式次数写成 `m+1`，对平方数/三角形数给出 `n³`。
  正确公式是 `m`（`(E−1)^m a = d` 需要 m 次多项式）。与窗口拟合的对账报出
  `deg=3 vs 2.0` 后改正；平方数现为 `deg=2`，与窗口值 2.0 一致。
- **解析器一元负号优先级**：`-x^2` 曾被求成 `(-x)^2`。`parse_factor` 改为先吃 `^`、
  再由 `parse_unary` 处理一元负号，现正确求为 `−(x^2)`。
- **`ln` 负实参未回退复数**：`ln(-3)` 直接抛错，导致 6 条反函数恒等式被误判。
  `_call_func` 已加 `cmath.log` 回退分支。
- 上述三处修复救回了 6 条被误拒的真恒等式（arccos / arcsec / arccsc / arccsc(-z) /
  arcsech / arccoth）；标准测试集 18/18 不退化。
- **校准表写错（机器是对的）**：`n²` 的二阶差分是 2 而不是 0（三角形数是 1）。
  初版校准表写成 0，机器算出 2 反被当成未识别候选。已改参考值，未改机器。

### 新增（2026-09-19，AI 流水线产物）
- **06-AI自动化/02-引擎/openmath_sys/theoryforge.py**：理论锻造引擎。在五族有限对象
  （整数 / 图 / 有限群 / 整数划分 / 有限链复形）上以**精确有理算术**跑三条关系发现通道
  （线性零空间 / 单项式 / Graffiti 式不等式），并做**留出集**与**规模外推**两级反例搜索。
- **06-AI自动化/01-工作流/openmath_theory.py**（流水线第 7 阶段 S7）：产出
  `09-数据/theory_forge.json`、`theory_candidates.json`、`theory_net.json`
  与 `01-工作流/THEORY_REPORT.md`；含 6 项自核验（本轮 0 违规）。
- `openmath_sys/__init__.py` 补充导出 `structure` 与 `theoryforge` 两组能力。
- 每日自动流水线由 6 阶段扩展为 **7 阶段**（追加 S7_theory；它不消费任何上游产物，
  故 `meta` 计算的算子闭包不动点仍为第 4 轮，闭包 artifact 增至 24 类）。

### 修正（2026-09-19）
- **实错修正**：`structure.py::analyze_group` 此前把群的**指数**取成元素阶的**最大值**
  （对 S₃ 得出 3，正确值应为 lcm(2,3)=6）。现已改为最小公倍数，旧口径保留在
  新增的 `max_element_order` 字段中，**未静默抹掉历史取值**。依赖它的
  `structure_experiments.json` 已重新生成。
- 清理 `openmath_meta.py` 文档字符串中的乱码残片。

---

## [0.1.0] - 2026-09-10

项目骨架建立。

### 新增
- **00-宪章**：总纲、诚实红线、证据等级标准（L0–L6）、标识与编号规范、中英术语表、治理机制。
- **01-数学体系**：16 个数学分支，按 MSC 2020 映射，每分支含范围、核心对象、域间接口、开放问题入口。
- **02-公式库**：原子化公式记录 schema 与示例（欧拉恒等式、Riemann 函数方程）。
- **03-难题与猜想**：千年大奖难题、希尔伯特 23 问题、已解决世纪难题、经典未解猜想、反例库的档案结构与条目模板。
- **04-数学统一场论**：12 个统一纲领的范式清单、假设空间、跨纲领翻译表、不可通约分析、可证伪预测、评估矩阵。
- **05-验证中心**：L0–L6 验证协议、引擎插件规范、运行时与基线回归、探针（反例搜索）、形式化绑定目录。
- **06-AI自动化**：专家联盟角色卡（12 个角色）、研究流水线 SOP、提示词库、模型路由、人类在环、**防幻觉协议**。
- **07-社区**：默认中文的全球化社区结构与 i18n 政策。
- **08-工具**：`openmath` CLI（`doctor` / `lint` / `verify` / `index` / `status` / `new`），核心纯标准库可运行。
- **09–99**：数据、文献、教育、归档、沙箱目录。

### 新增
- **05-验证中心/01-引擎**：`prime_pi_block.py` —— 精确素数计数引擎（brute / Legendre / Meissel / Lehmer + primorial wheel 四路交叉），纯标准库，自检 6/6 通过。
- **05-验证中心/01-引擎/engines.yaml**：引擎注册表（插件化，新增引擎无需改核心代码）。
- **02-公式库/数论**：`OM-F-NT-0001` Legendre φ 分块递归与轮积恒等式、`OM-A-NT-0001` Meissel–Lehmer 精确素数计数（均 `VERIFIED(L2)`，含结构化 `checks`）。
- **03-难题与猜想/05-经典未解猜想**：`OM-P-NT-0002` 第 n 个素数的低复杂度精确公式（Q1 已解决 / **Q2 开放**，`fact_check: UNVERIFIED` 诚实标注）。

### 说明
本版本**不含大规模内容填充**。分支内容与难题档案目前仅有骨架与示例，属于有意为之：先固化结构与证据标准，再批量录入。
