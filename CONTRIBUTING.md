# 贡献指南

> 默认语言：简体中文。若你更习惯英文，可以直接用英文提交，翻译组会补齐中文层；**但记录文件中的 `id`、`status`、`evidence_level` 等机器字段必须使用规范枚举值，不得翻译。**

## 一、三分钟流程

```bash
git checkout -b feat/OM-F-NT-0042
cp 02-公式库/_模板/公式模板.yaml 02-公式库/数论/OM-F-NT-0042.yaml
# 编辑...
python -m openmath lint      # 必须零错误
python -m openmath verify --level L2 --id OM-F-NT-0042
git add . && git commit -m "[NT] 新增 xxx 公式"
```

## 二、你可以贡献什么

| 类型 | 位置 | 门槛 | 说明 |
| --- | --- | --- | --- |
| 修正错别字 / 补充参考文献 | 任意 | 低 | 直接 PR |
| 新增公式记录 | `02-公式库/` | 低 | 需给出至少一个可执行的验证断言 |
| 新增难题条目 | `03-难题与猜想/` | 中 | 需注明权威来源与当前状态 |
| 新增分支内容 | `01-数学体系/` | 中 | 先在该分支 README 的"待办清单"登记 |
| 新增验证引擎 | `05-验证中心/01-引擎/` | 高 | 需提交能力声明与自检样例 |
| 新增统一纲领 | `04-数学统一场论/01-范式清单/` | 高 | 必须填写假设空间与可证伪预测 |
| 形式化证明 | `05-验证中心/07-形式化/` | 高 | 需给出可复现构建命令 |

## 三、硬性要求（CI 会拦截）

1. **ID 唯一且格式合法**：见 [编号规范](00-宪章/04-标识与编号规范.md)。
2. **必填字段完整**：`id`、`title`、`domain`、`status`、`evidence_level`、`provenance`。
3. **假设显式化**：定理/公式记录中 `assumptions` 不得为空；无假设则写 `assumptions: []` 并在备注说明为何为空。**隐含假设视为缺陷。**
4. **禁止结论漂移**：不允许为了让测试通过而修改断言。**测试失败时，正确做法是修正内容或把状态改为 `DISPUTED` / `FALSIFIED`。**
5. **AI 生成内容必须标注**：`provenance.ai_assisted: true`。未标注即视为违规。
6. **可复现**：数值结果需记录精度（`precision_digits`）、随机种子（`seed`）、依赖版本。

## 四、提交信息规范

```
[域代码] 动词 + 对象

动词限于：新增 / 修正 / 重构 / 证伪 / 形式化 / 翻译 / 归档
```

示例：

```
[NT] 新增 Mertens 函数上界公式
[GT] 证伪 OM-C-GT-0007（给出 12 顶点反例）
[MF] 形式化 OM-T-MF-0003（Lean 4，mathlib 2026-09）
```

## 五、评审标准

评审者只问四个问题：

1. **证据是否匹配声明的等级？**（声明 L4 却没有形式化脚本，直接退回）
2. **假设是否写全？**
3. **能否被证伪？**（不可证伪的表述应移入 `99-沙箱/`）
4. **是否重复？**（先跑 `python -m openmath index --search "关键词"`）

不比资历，只比证据。参见 [诚实红线](00-宪章/02-诚实红线.md)。

## 六、行为准则

参与即视为同意 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)。要点：批评只针对论证，不针对人；承认错误是加分项；"我错了"永远不需要道歉，掩盖错误才需要。
