# 2026-09-22 工作日志

## xiaobai_voice 独立开源到 GitHub
- 源位置：D:/a10/aikjx/gitcode/infotopograph/projects/xiaobai_voice（infotopograph = aikjx/mox 仓库的本地克隆，含 gitcode + github 两远端）。
- 做法：复制源码到新位置 D:/a10/aikjx/gitcode/xiaobai_voice（robocopy 排除 target/__pycache__/.venv/dist/*.pyc 等），加 .gitignore + Apache-2.0 LICENSE，git init 后推送；未改动 mox 仓库。
- 结果：https://github.com/aikjx/xiaobai_voice 公开、main 分支、65 文件、提交人 aikjx（b790911）。
- 推送沿用 credential.helper 重置为 store 的方式绕过 helper-selector。
- 遗留：mox 内仍有同一份 xiaobai_voice 子目录（未解绑）；README 仍含 mox 品牌痕迹（配置路径含 mox/xiaobai、作者 Mox Team）。

## 品牌改名：xiaobai_voice -> xiaobai（定位：每个人的 AI 伙伴与助手）
- 决策：品牌用角色名「小白(Xiaobai)」，voice 降为能力模块名（Python 包名不改，代码不动）。
- 执行：PATCH /repos/aikjx/xiaobai_voice 改名为 xiaobai（200）；旧链接 301 重定向仍有效。
- 本地目录同步：gitcode/xiaobai_voice -> gitcode/xiaobai；origin 改为 xiaobai.git。
- README 顶部改为「小白 · 每个人的 AI 伙伴与助手」，突出离线/隐私；提交 b1f736d。
- 仓库 description 与 topics 已更新。新链接：https://github.com/aikjx/xiaobai

## mox 品牌痕迹向后兼容清理（提交 c8571a8，13 文件 +127/-56）
- 原则：**只迁移「对外品牌」，保留「后端集成协议」**。改品牌会让用户丢配置/模型路径，属于破坏性变更，故一律加 legacy 回退。
- 保留不动（改了会断与 mox 后端互通）：`mox-system`/`mox-expert` 桥、`MoxAdmin` RBAC 角色名、`voice_proxy` 端口 13130 协议。
- 对外品牌迁移点：
  - pyproject 作者 Mox Team -> aikjx；描述改为「小白（Xiaobai）· 每个人的 AI 伙伴与助手」。
  - 桌面端 `setOrganizationName("Mox")` -> `"Xiaobai"`；主窗口标题、toast「mox 桥离线」->「云端桥离线」。
  - 配置/日志/模型目录：新增 `xiaobai`（`%APPDATA%\xiaobai`、`~/.xiaobai/models/voice`），保留 `mox/xiaobai`、`~/.mox/...` 为 **legacy 回退**（新路径不存在且旧路径存在时自动沿用旧路径）。
  - 环境变量：新增 `XIAOBAI_VOICE_PORT`/`XIAOBAI_VOICE_CONFIG`/`XIAOBAI_TEMP`，**同时仍注入** `MOX_*` 旧变量；端口读取优先新变量再回落旧变量。
  - Rust 侧 `xiaobai_core/src/config/mod.rs` 同步实现 APP_DIR/LEGACY_APP_DIR 与回退；ASR/TTS 模型候选目录同样新路径优先、旧路径兜底。
- 验证：Python `compileall` exit=0；`cargo check` exit=0（仅既有 warning，与本次无关）。
- 推送：`b1f736d..c8571a8 main -> main`，用 `git -c credential.helper= -c credential.helper=store` 绕过 helper-selector。
- 同步核验（权威）：`git ls-remote origin refs/heads/main` 与本地 HEAD 均为 c8571a8，工作区干净。
- 仍开放（未做，等用户决定）：是否从 mox 仓库解绑该子目录（infotopograph 内仍存一份）；是否彻底去掉 mox 后端桥引用（**会破坏与 mox 的互通**，不建议）。
