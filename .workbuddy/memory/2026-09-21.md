# 2026-09-21

## 改动存活性核查 + S8 审计验证

### 1. 核查并发改动是否回滚了我方修复
逐项 grep 引擎源码，四项修复**全部完好**：
- `parser.py::split_quantifier`（量词前缀）
- `parser.py::_SENTENCE_TAIL_RE`（尾随句读）
- `parser.py::parse_juxta` 的「内置函数名紧随 -> 连写乘法」
- `parser.py::parse_atom` 的 VAR 分支并列函数应用
- `models.py::MathExpr.quantified` 字段

结论：未被其他会话回滚。但仍应把"核查存活性"作为每次续做的第一步。

### 2. 跑 S8 独立审计（`06-AI自动化/01-工作流/openmath_audit.py`）
- **4452 项检查全部通过，0 失败**；审计自核验 6 项全过。
- 意义：证明解析器改动未破坏仓库不变量。尤其恒等式验证器 117 项通过——
  该项与「函数名/散文词被当变量」的误判直接相关，是最容易被我的并列函数应用
  改动波及的部分，它通过说明改动是安全的。
- 审计报告 `06-AI自动化/01-工作流/AUDIT_REPORT.md` 第 4 节记录了历史缺陷
  （F1 高: 超出能力范围的式子被误判真伪；F2/F3 中；F4 参考值记错），
  第 5 节列了审计自身盲区（参考值可能错、两实现可能错到一起、对象集小、
  不覆盖语义层、通过率会随时间失真）。引用审计结论前应读这两节。

### 3. 剩余失败模式量化（决定不再强推扩展）
77 条未解析样本中：**含散文词 41 条**（本质非方程，不应强解析）；
其余为布尔逻辑 10、下标 9、LaTeX 7、链式关系 7（去散文后）、lambda 3、区间 1。
- 关键判断：**没有单一扩展能带来显著增益**，且布尔逻辑（and/or/xor 中缀）
  与散文共享同一批词，强推会把英文散文误判成表达式 —— 属静默错误，风险高于收益。
- 结论：算术/代数解析器已到合适的**能力边界**。继续追可解析率会诱导
  "把散文也算作解析成功"的错误优化，应停止。

### 待办 / 建议
- 若要继续提升解析能力，优先做语义明确、无散文歧义的项：
  链式关系 `a < b <= c`（数学上等价于合取），以及绝对值 `|a|`。
- 不建议做：布尔中缀连接词、lambda、LaTeX、下标记号（收益小、误判风险高）。
