# OM-P-NT-0007 · 单矩判据的渐近存活性与展开预算（2026-09-26）

> 状态：**OPEN**。承接 OM-P-NT-0006。
>
> 单矩判据的渐近存活性——**两路结果必须分开读**：
> * 盲外推路线：**UNDETERMINED**（自校验不过，读数作废；方法论负面结果，保留）。
> * 密度模型路线：**ALIVE**，`kappa_1(inf) = (log 2)^2 - 1 = -0.5195 < 0`（启发式模型结论，非定理）。
>
> 下面第一到三节记录外推路线（失败），三-b 节给出密度模型（正面结果）。

## 一、待答问题

OM-P-NT-0006 给出**单矩充分判据** `M_1 < 0 ==> a_11 >= 1 ==> Goldbach`，
但它只在 `N <= 6e4` 被扫描过，且 `kappa_1` 序列**单调趋向 0**。
本轮回答：这个趋势会不会最终穿越 0？

## 二、块 1 · kappa 趋势

### N_mod6_eq_0

| N | kappa_x | kappa_w(实测) | kappa_1 | c_B(N) | prod R_p | kappa_w(模型) | 实测/模型 |
|---|---|---|---|---|---|---|---|
| 3000 | 1.082 | 0.094 | -0.988 | 0.4003 | 0.9005 | 0.1443 | 0.649 |
| 6000 | 1.184 | 0.174 | -1.010 | 0.4451 | 0.9324 | 0.1848 | 0.941 |
| 12000 | 1.212 | 0.186 | -1.027 | 0.4869 | 0.9547 | 0.2263 | 0.822 |
| 24996 | 1.227 | 0.211 | -1.016 | 0.4857 | 0.9666 | 0.2281 | 0.926 |
| 49998 | 1.238 | 0.284 | -0.953 | 0.5477 | 0.9793 | 0.2938 | 0.968 |
| 99996 | 1.197 | 0.258 | -0.939 | 0.5325 | 0.9835 | 0.2789 | 0.924 |
| 199998 | 1.176 | 0.311 | -0.865 | 0.5686 | 1.0014 | 0.3238 | 0.960 |
| 399996 | 1.188 | 0.296 | -0.892 | 0.5396 | 1.0054 | 0.2927 | 1.012 |
| 799998 | 1.161 | 0.324 | -0.837 | 0.5722 | 1.0129 | 0.3316 | 0.976 |
| 1599996 | 1.167 | 0.321 | -0.847 | 0.5650 | 1.0139 | 0.3237 | 0.991 |
| 3000000 | 1.140 | 0.334 | -0.805 | 0.5907 | 0.9967 | 0.3478 | 0.961 |

### N_mod6_eq_2

| N | kappa_x | kappa_w(实测) | kappa_1 | c_B(N) | prod R_p | kappa_w(模型) | 实测/模型 |
|---|---|---|---|---|---|---|---|
| 2996 | 1.212 | 0.108 | -1.104 | 0.4008 | 0.9005 | 0.1446 | 0.745 |
| 5996 | 1.204 | 0.191 | -1.013 | 0.4454 | 0.9324 | 0.1850 | 1.033 |
| 11996 | 1.203 | 0.234 | -0.969 | 0.4870 | 0.9547 | 0.2264 | 1.033 |
| 24998 | 1.215 | 0.198 | -1.017 | 0.4857 | 0.9666 | 0.2280 | 0.866 |
| 50000 | 1.115 | 0.256 | -0.859 | 0.5477 | 0.9793 | 0.2938 | 0.870 |
| 99998 | 1.210 | 0.305 | -0.905 | 0.5325 | 0.9835 | 0.2789 | 1.094 |
| 200000 | 1.180 | 0.309 | -0.871 | 0.5686 | 0.9890 | 0.3198 | 0.966 |
| 399998 | 1.191 | 0.279 | -0.912 | 0.5396 | 0.9909 | 0.2885 | 0.965 |
| 800000 | 1.158 | 0.313 | -0.845 | 0.5722 | 0.9939 | 0.3254 | 0.961 |
| 1599998 | 1.173 | 0.321 | -0.852 | 0.5650 | 0.9953 | 0.3177 | 1.011 |
| 2999996 | 1.159 | 0.345 | -0.814 | 0.5907 | 1.0046 | 0.3505 | 0.985 |

`c_B(N) = B(N)·logN/N`，`B(N) = #{Omega=2 且 spf > N^{1/3}}`（口径 X = N）；
`kappa_w(模型) = c_B(N)^2 · prod R_p`；`prod R_p` 是 B+B 的局部（奇异级数）修正比。
**读法**：`实测/模型` 趋向 1，说明该模型是本轮外推失败后的正确替代。

## 三、块 1 · 1/log N 外推

同时做**线性**（`c0 + c1/logN`）与**二次**（`+ c2/log^2N`）两种拟合，
取截距 `c0` 为极限估计。二次拟合外推可能失真，故线性值作为稳健性对照；
只有当两者同号时才采信。

| 序列 | 拟合 | kappa_x(inf) | kappa_w(inf) | kappa_1(inf) |
|---|---|---|---|---|
| N_mod6_eq_0 | 二次 | 0.093 | 0.338 | 0.245 |
| N_mod6_eq_0 | 线性 | 1.201 | 0.612 | -0.589 |
| N_mod6_eq_2 | 二次 | 1.138 | 0.345 | -0.793 |
| N_mod6_eq_2 | 线性 | 1.109 | 0.580 | -0.529 |

**自校验说明**：Hardy–Littlewood 预言 `kappa_x(inf) = 1`。
若外推能把 `kappa_x` 恢复到 1 附近，则同一方法对 `kappa_w` 的读数才有参考价值。

| 序列 | |kappa_x(inf)-1|（二次 / 线性） | 外推可信 |
|---|---|---|
| N_mod6_eq_0 | 0.907 / 0.201 | False |
| N_mod6_eq_2 | 0.138 / 0.109 | True |

| 序列 | kappa_1(inf) 的两种读数区间 | 线性存活 | 二次存活 |
|---|---|---|---|
| N_mod6_eq_0 | [-0.589, 0.245] | True | False |
| N_mod6_eq_2 | [-0.793, -0.529] | True | True |

### 裁定

外推自校验**至少有一条序列未通过**（`kappa_x(inf)` 偏离理论值 1 过远，见上表）=>
该序列的 `kappa_1(inf)` 读数**作废**；总体判定 **UNDETERMINED**：
既不能称单矩判据渐近存活，也不能称其报废。
符号随拟合阶数改变、因而未定的序列：`['N_mod6_eq_0']`。
要判定需要更大尺度（`N >= 1e8`）或 `kappa_x` / `kappa_w` 的解析闭式。

> 上表是**外推路线**的结果。外推在本轮**未能**定出符号；
> 下面改用**可解析求极限的密度模型**，答案才落地。

## 三-b、块 1c · 解析极限（本轮正面结果）

换掉盲外推：`kappa_w` 有闭式的**密度模型**。用标准奇异级数形式
（对素数集该式给出 Hardy–Littlewood 的 `#{p1+p2=N} ~ S(N)A(N)^2/N`，
故归一化正确）：

$$w=\#\{b_1+b_2=N,\ b_i\in B\}\ \sim\ \mathfrak S_B(N)\cdot\frac{B(N)^2}{N}$$

于是 `kappa_w(N) = (S_B/S_prime)·c_B(N)^2 = (prod_p R_p)·c_B(N)^2`，其中
`R_p := F_p^B/F_p^prime` 由局部密度直接算出。两个关键事实：

* `p <= z` 时 `theta_p = 0`（`B` 的元素素因子全 `> z`）=> `R_p = 1` **精确**；
  `p > z` 时 `R_p - 1 = O((theta_p^2+1)/p^2)` => `prod_p R_p -> 1`。
* `c_B(N) -> log 2`。推导：`B(N) = sum_{N^{1/3}<p<=sqrt N}(pi(N/p)-pi(p))`
  `~ (N/log N) sum 1/(p(1-u_p))`，`u_p = log p/log N`；以 `1/p` 为权时 `u` 均匀于 `[1/3,1/2]`，
  故 `sum -> int_{1/3}^{1/2} du/(u(1-u)) = [ln(u/(1-u))]_{1/3}^{1/2} = ln 2`。

$$\boxed{\ \kappa_w(\infty)=(\log 2)^2\approx 0.4805,\qquad
\kappa_x(\infty)=1,\qquad \kappa_1(\infty)=(\log 2)^2-1\approx -0.5195\ <\ 0.\ }$$

| 序列 | c_B 拟合极限（二次 / 线性） | log 2 | 最后实测 c_B | 误差(二次 / 线性) |
|---|---|---|---|---|
| N_mod6_eq_0 | 0.5379 / 0.7932 | 0.6931 | 0.5907 | 0.1553 / 0.1000 |
| N_mod6_eq_2 | 0.5401 / 0.7926 | 0.6931 | 0.5907 | 0.1530 / 0.0994 |

`prod R_p` 首行 / 末行：`{'N_mod6_eq_0': 0.9005122824939324, 'N_mod6_eq_2': 0.9005122824939324}` / `{'N_mod6_eq_0': 0.9966937647482125, 'N_mod6_eq_2': 1.004647790876656}`（尾部已趋 1；尾部判据 = True）。
最后几行的 `实测/模型`（应为 1）：`{'N_mod6_eq_0': [0.976139270005092, 0.9907758621066811, 0.9612228939411378], 'N_mod6_eq_2': [0.961429137059567, 1.0105983538585486, 0.9850753794691395]}`
`实测/模型` 的**全程**范围：`[0.648822172109222, 1.0944365703275272]` —— 未一直落在 1 上，
系统偏差约 3~4%，来源是把 `B` 的密度当作在 `[1,N]` 上均匀（实际 `rho_B` 在靠近 `N` 处偏高）。
该偏差对 `kappa_1(inf) < 0` 的结论无影响（余量 ~0.52 远大于 4%）。

**c_B 的检验要说清强弱**：两种拟合 0.538 / 0.793 只是把 `log 2 = 0.6931`
**夹在中间** —— 数值**与** `log 2` 一致，但**不能确认**它
（相邻 N 的 c_B 抖动可达 0.03）。`log 2` 的地位是**解析导出**，数值仅提供一致性。

**结论**：`kappa_1(inf) = (log 2)^2 - 1 = -0.5195 < 0`，
即在标准密度（Hardy–Littlewood 型）启发式下，OM-P-NT-0006 的**单矩判据在渐近意义下存活**。
余量不是边缘的：即使 `kappa_w(inf)` 有 30% 的模型误差，`kappa_1(inf)` 仍为负。
**但这是启发式模型，不是证明**：`kappa_x(inf)=1` 与 `c_B(inf)=log 2` 都用到了
Hardy–Littlewood 型的独立性假设，而正是该假设在奇偶障碍处失效。

## 四、块 2a · M_1 的 Möbius / AP 展开核对

精确恒等式（本轮暴力核对）：

$$M_1=\sum_{d\mid P(z)}\mu(d)\,\mathrm{Inner}(d),\qquad
\mathrm{Inner}(d)=\sum_{\substack{1\le n<N\\ d\mid n(N-n)}}\lambda(n)$$

$$\mathrm{Inner}(d)=\sum_{d_1d_2=d/e}\lambda(e d_1)
\sum_{\substack{a\le (N-1)/(e d_1)\\ a\equiv N(e d_1)^{-1}\ (d_2)}}\lambda(a),
\quad e=\gcd(d,N)$$

| N | z | 除数个数 | M_1 直接 | M_1 经 Möbius | Möbius 精确 | AP 分解精确 |
|---|---|---|---|---|---|---|
| 1000 | 7 | 16 | -50 | -50 | True | True |
| 1000 | 11 | 32 | -52 | -52 | True | True |
| 1000 | 13 | 64 | -54 | -54 | True | True |
| 2000 | 7 | 16 | -48 | -48 | True | True |
| 2000 | 11 | 32 | -60 | -60 | True | True |
| 2000 | 13 | 64 | -60 | -60 | True | True |
| 4000 | 7 | 16 | -98 | -98 | True | True |
| 4000 | 11 | 32 | -100 | -100 | True | True |
| 4000 | 13 | 64 | -112 | -112 | True | True |

关键点：`p | N` 时 `p|n <=> p|N-n`，该类素因子必须归入 `e` 而不是互补因子对，
否则重复计数 —— 暴力核对正是为了排除这个错误。

## 五、块 2b · lambda 在等差数列中的偏差 vs 预算

| x | q | max|E(x;q,a)| | 平凡界 x/q | 对平凡界 | 目标预算 x/log²x | 对预算 |
|---|---|---|---|---|---|---|
| 100000 | 1 | 288 | 100000 | 0.003 | 754 | 0.4 |
| 100000 | 2 | 376 | 50000 | 0.008 | 754 | 0.5 |
| 100000 | 3 | 377 | 33333 | 0.011 | 754 | 0.5 |
| 100000 | 6 | 322 | 16667 | 0.019 | 754 | 0.4 |
| 100000 | 7 | 156 | 14286 | 0.011 | 754 | 0.2 |
| 100000 | 30 | 175 | 3333 | 0.052 | 754 | 0.2 |
| 100000 | 210 | 68 | 476 | 0.143 | 754 | 0.1 |
| 100000 | 2310 | 26 | 43 | 0.601 | 754 | 0.0 |
| 1000000 | 1 | 530 | 1000000 | 0.001 | 5239 | 0.1 |
| 1000000 | 2 | 916 | 500000 | 0.002 | 5239 | 0.2 |
| 1000000 | 3 | 1005 | 333333 | 0.003 | 5239 | 0.2 |
| 1000000 | 6 | 874 | 166667 | 0.005 | 5239 | 0.2 |
| 1000000 | 7 | 367 | 142857 | 0.003 | 5239 | 0.1 |
| 1000000 | 30 | 479 | 33333 | 0.014 | 5239 | 0.1 |
| 1000000 | 210 | 198 | 4762 | 0.042 | 5239 | 0.0 |
| 1000000 | 2310 | 87 | 433 | 0.201 | 5239 | 0.0 |

**取值范围披露（重要）**：本轮只测 `x <= 1e6`、`q <= 2310`。
更大的 `q`（例如 `q = 10000`）会让每个剩余类只剩约 10 个样本；
而 `ratio_to_trivial = max|E(x;q,a)| / (x/q)` 有**硬上界 1**
（类内元素个数即 `x/q`，故 `|和| <= 类内元素个数`），样本一少就轻易触顶 ——
那是**小样本假象**，不是真实偏差。早期版本把 `q = 1000, 10000` 计入，
曾据此报出 `ratio_to_trivial = 1.0`，已剔除。
另外，展开式中真正出现的模是 `d2 <= P(z)`（本轮 `z = N^{1/3}`），
远小于上表的 2310；上表是大范围的经验探测，不是该展开的精确模范围。

## 六、裁定

* 【块 1c · 本轮正面结果（首选答案）】盲外推失败后改用**可解析求极限的密度模型**：kappa_w(N) = (prod_P R_p) * c_B(N)^2，其中 c_B(N) = B(N)logN/N，R_p 由局部密度精确算出。关键：p <= N^{1/3} 时 R_p = 1 精确（B 的元素素因子全 > N^{1/3}），p > N^{1/3} 时 R_p - 1 = O(1/p^2)，故 prod R_p -> 1；而 c_B(N) -> log 2（可解析推出）。于是 kappa_w(inf) = (log 2)^2 = 0.4805，kappa_x(inf) = 1（Hardy-Littlewood），**kappa_1(inf) = (log 2)^2 - 1 = -0.5195 < 0**。故单矩判据 M_1 < 0 在渐近意义下**存活**，且余量非边缘（kappa_w(inf) 即便有 30% 模型误差，kappa_1(inf) 仍为负）。数值支持：obs/model 的最后三行 = {'N_mod6_eq_0': [0.976139270005092, 0.9907758621066811, 0.9612228939411378], 'N_mod6_eq_2': [0.961429137059567, 1.0105983538585486, 0.9850753794691395]}（趋向 1），prod R_p 的末行值趋向 1。
* 【块 1c · 但必须说清这是什么强度】上述推导用到两处 Hardy-Littlewood 型独立性假设（kappa_x(inf)=1 与 c_B(inf)=log 2 的局部化）。**该假设恰恰是奇偶障碍所在之处**，因此这是启发式模型结论，不是定理。它的作用是：把'单矩判据是否值得作为归约目标'从**未知**变成**有明确正面指示**，而不是把 Goldbach 往前推了一步。
* 【块 1 · 外推路线被否掉（方法论负面结果，保留）】原本想靠 1/log N 多项式外推定符号，但自校验把这条路否掉了：Hardy-Littlewood 预言 kappa_x(inf) = 1，而外推给出的读数为 N_mod6_eq_0：二次 0.093（误差 0.907）、线性 1.201（误差 0.201）；N_mod6_eq_2：二次 1.138（误差 0.138）、线性 1.109（误差 0.109）（阈值 0.25）。一条序列的二次拟合把 kappa_x(inf) 外推到 0.09（明显过拟合），两条序列的 kappa_1(inf) 区间 {'N_mod6_eq_0': [-0.5889731320930988, 0.24510082810682235], 'N_mod6_eq_2': [-0.7931321022519549, -0.528782939332855]} 甚至异号。**故 extrapolation_route_status = UNDETERMINED，外推读数一律作废。**教训：对 kappa 这类含 loglog / 对数低阶项的量，在 1/log N 跨度仅 2 倍时做二次外推不可靠。
* 【块 1 · 外推自校验的完整记录】两道自校验：HL_selfcheck_per_series = False（阈值 0.25，字段 extrapolation_selfcheck_HL_kappa_x_limit_1）；符号随拟合阶数改变、因而**未定**的序列为 ['N_mod6_eq_0']。kappa_w 两条序列读数稳定（约 0.34），不稳定主要来自 kappa_x。
* 【判据的定义（与判定分开陈述）】若 kappa_1(inf) < 0，则 OM-P-NT-0006 的单矩判据（M_1 < 0 ==> Goldbach）对所有充分大的偶数 N 成立，是一条合法的（虽然奇偶敏感的）归约目标；若 >= 0，则该判据作为充分条件报废，必须退回完整三矩。**本轮给出的是该条件的经验状态，不是证明。**
* 【块 2a】M_1 的 Mobius 展开与 CRT/AP 分解在全部测试案例上**逐项精确成立**，（注：p | N 时 p|n <=> p|N-n，故该类素因子归入 e 而非互补因子对 —— 这一步若弄错整个分解会重复计数，本轮的暴力核对正是为了排除这种错误）。这保证后面的预算推演建立在真恒等式之上。
* 【块 2b 预算 · 平凡界】单个 Inner(d) 的**平凡界**是 N/d，而 M_1 的目标尺度只有 ~0.8*S(N)N/log^2 N：连 d=1 那一项的平凡界都比目标大 ~log^2 N 倍。因此必须靠**跨 d 的抵消**实现 log^2 N 量级的压缩。
* 【块 2b 实测 · 障碍的性质必须说准】实测的 lambda 在 AP 中的和max|E(x;q,a)| **低于**目标预算，而不是高于：相对目标预算的最大比值仅 0.500，相对平凡界的最大比值 0.601。所以障碍**不是**'每一项都远大于目标'这种数值大小问题，而是**没有任何已知方法能证明**这些 AP 和有一致的常数因子节省（Selberg 奇偶障碍）—— 这是证明障碍，不是数值障碍。把实测的'小'读成'已解决'同样是错的：有限尺度的实测不构成任何渐近断言。
* 【这不是偷换了对象】展开后出现的正是sum_{a <= N/(e*d1), a = c mod d2} lambda(a)，即 **lambda 在等差数列中的和**；它与上一轮的 M_1 = sum lambda(n) 属**同一类对象**（后者是 q = 1 的特例）。所以'换成 AP 版'不会带来难度上的便宜，也不存在可供偷换的黑箱。

## 七、仍然开放

* kappa_1(inf) = (log 2)^2 - 1 < 0 是**启发式模型**结论：两处输入（kappa_x(inf)=1 与 c_B(inf)=log 2）都依赖 Hardy-Littlewood 型独立性假设，而这正是奇偶障碍所在。要把它变成定理，本质上就是要突破奇偶障碍。
* **数值无法确认 c_B(inf) = log 2**：实测 c_B(N) 在 N = 3e6 处仍只有 0.5907（log 2 = 0.6931），且两种拟合给出 0.538 与 0.793，只是把 log 2 **夹在中间**；数据本身有 O(1/log N) 量级的抖动（相邻 N 可差 0.03）。所以 log 2 的地位是**解析导出的猜测**，数值仅提供一致性，不是确认。同理 kappa_w(inf) = (log 2)^2 无法在可达尺度上直接检验。
* 模型对 kappa_w 的系统偏差约 3-4%：obs/model 的全程范围为 [0.648822172109222, 1.0944365703275272]（未一直落在 1 上），来源是把 B 的密度当作在 [1,N] 上均匀 —— 实际 rho_B 在靠近 N 处偏高。该偏差对 kappa_1(inf) < 0 的结论无影响（余量 ~0.52 远大于 4%）。
* 多项式外推在 1/log N 跨度仅 2 倍时不可靠（本轮自校验已证）；若坚持纯数值路线，需要 N >= 1e8 级别并配以正确的低阶项形式。
* 即便 kappa_1(inf) < 0 得到确认，'证明 M_1 < 0'本身仍是一个未解决的奇偶敏感问题：现有筛法对它给出 0 而非任何常数因子的节省。
* 块 2b 的 level-of-distribution 是**经验**测量，且本轮只取 x <= 1e6、q <= 2310（更大的 q 会因每类样本数过少而失真）；它描述的是 lambda 在有限尺度的实际行为，不构成定理。
